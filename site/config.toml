# The URL the site will be built for
base_url = "https://dev.distyopoly.xyz"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

title = "🂠 Distyopoly Hub"
theme = "tabi"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "css"
external_links_class = "external"



[extra]
# favicon="favicon.ico"
favicon_emoji="🃏"

# Put all your custom variables here
skin="red"

menu = [
    {name = "docs", url = "#"},
    {name = "#devlog", url = "devlog"},
    {name = "repók", url = "repos"}
]

socials = [
    { name = "github", url = "https://github.com/orgs/Distyopoly/repositories", icon = "github" },
    { name = "npmjs", url = "https://www.npmjs.com/org/distyopoly", icon = "npm-2009" },
]


remote_repository_url = "https://github.com/Distyopoly/disty-paper/tree/main/site"
remote_repository_git_platform = "auto"
remote_repository_branch = "main"
show_remote_changes = true
show_remote_source = true